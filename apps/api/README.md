# API application (`apps/api`)

**Status:** directory layout only — **NestJS not initialized** (no `package.json`, no compile step).

Module folders under `src/modules/` match the Auryn backend architecture spec. Implementation starts only after foundation sign-off.

See `REPOSITORY-STRUCTURE.md` and `docs/architecture/backend-architecture.md`.
