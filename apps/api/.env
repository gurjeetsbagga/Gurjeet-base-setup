# Local development — Prisma reads .env from the prisma directory's parent.
# For the full env configuration, see the root .env.example.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auryn_dev
DATABASE_DIRECT_URL=postgresql://postgres:postgres@localhost:5432/auryn_dev

# OpenAI (server-only — never expose to web/mobile)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
